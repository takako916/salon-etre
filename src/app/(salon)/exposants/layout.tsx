import { salonMetadata } from "@/lib/salon-seo";

export const metadata = salonMetadata.exposants;
export default function ExposantsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
